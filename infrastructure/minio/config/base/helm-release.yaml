apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minio-tenant
spec:
  interval: 30m
  chart:
    spec:
      chart: tenant
      version: "set-in-overlay"
      sourceRef:
        kind: HelmRepository
        name: minio-operator
      interval: 12h
   