# Tempo
For local development purposes one should the monolithic mode. This is a single pod that runs Tempo and doesn't scale.
For live clusters use the microservices mode with kind TempoStack.
More info found here: https://grafana.com/docs/tempo/latest/setup/operator/monolithic/