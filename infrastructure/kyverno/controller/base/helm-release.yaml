apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kyverno-controller
spec:
  interval: 30m
  chart:
    spec:
      chart: kyverno
      version: "3.4.4"
      sourceRef:
        kind: HelmRepository
        name: kyverno
      interval: 12h
