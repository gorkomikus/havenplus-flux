apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alloy
spec:
  chart:
    spec:
      version: "0.7.0"
  values:
    controller:
      type: daemonset
    alloy:
      stabilityLevel: experimental
      configMap:
        name: alloy
        key: config.alloy
