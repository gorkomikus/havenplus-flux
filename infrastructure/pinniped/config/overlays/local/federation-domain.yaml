apiVersion: config.supervisor.pinniped.dev/v1alpha1
kind: FederationDomain
metadata:
  name: supervisor
spec:
  issuer: "https://pinniped-supervisor.local"
  tls:
    secretName: pinniped-supervisor-tls
