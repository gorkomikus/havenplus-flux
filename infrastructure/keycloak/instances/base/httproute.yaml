apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: keycloak
spec:
  parentRefs:
  - name: public-gateway
    namespace: istio-gateway
  hostnames:
  - set-in-overlay
  rules:
  - backendRefs:
    - name: keycloak-service
      port: 8080
